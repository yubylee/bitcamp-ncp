package com.eomcs.net;
import java.util.Arrays;
public class Practice {
  String str = "adskfjasdl"
      char[] arr = new char[str.length()];
  for(int i = 0; i < arr.length; i++) {
    arr[i] = str.charAt(i);
  }
}
